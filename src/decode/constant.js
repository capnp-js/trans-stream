/* @flow */

export const DECODE_INCOMPLETE_ERROR =
  "Cap'n Proto byte stream decode doesn't fill the space prescribed by the stream's header.";

export const DECODE_OVERFLOW_ERROR =
  "Cap'n Proto byte stream decode overflows the space prescribed by the stream's header.";
